# Virtual environments
venv/
__pycache__/
*.pyc

# Python build artifacts
*.egg-info/
*.egg
dist/
build/
*.whl

# Local configuration
.env
.env.local
settings.json
extensions.json

# Supabase local data and runtime state
.supabase/
.supabase-dev/
.supabase-test/
supabase/.temp/
supabase/.branches/
supabase/.runtime/

scripts/schema_public_remote.sql

# Test artifacts
*.log
*.tmp
test_output.txt
logs_*.zip
.coverage
.pytest_cache/
temp/*

# OS / IDE files
.DS_Store
Thumbs.db
.idea/
.vscode/

# Written by kankyouken-deploy/scripts/frontend-vm.ps1 while the frontend is pointed
# at the VM. Normally restored on exit, but a hard kill can leave it behind - and
# this repo is public, so it must never be committable.
.env.local.devbackup
.env.local.*
